name: Cycle normal VEILLE->OUVERTURE_CANON->REMPLISSAGE_POUMON->EN_COURS
version: 1
steps:
  - wait-serial:
      text: "machine initialisee"
      timeout: 15000
  - wait-serial:
      text: "0 -> 1"
      timeout: 3000
  - wait-serial:
      text: "1 -> 3"
      timeout: 12000
  - wait-serial:
      text: "3 -> 4"
      timeout: 8000
