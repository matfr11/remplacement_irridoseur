#include "webserver.h"
#include "esp_log.h"

// Implémentation complète — PR-08 / PR-09
static const char *TAG = "webserver";

esp_err_t webserver_init(void)
{
    ESP_LOGI(TAG, "WebServer — non implémenté (PR-08)");
    return ESP_OK;
}

void webserver_broadcast_status(void)
{
    // PR-08
}

void webserver_stop(void)
{
    // PR-08
}
