#include "ota.h"
#include "esp_log.h"

// Implémentation complète — PR-08
static const char *TAG = "ota";

esp_err_t ota_init(void)
{
    ESP_LOGI(TAG, "OTA handler — non implémenté (PR-08)");
    return ESP_OK;
}
